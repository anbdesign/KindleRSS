# RSS Kindle Reader Environment Configuration

# Base URL for the application (optional)
# If not set, the app will auto-detect from the request
# Examples:
# BASE_URL=https://your-domain.com
# BASE_URL=http://localhost:3000
# BASE_URL=https://rss-reader.example.com

# PORT (optional, defaults to 3000)
# PORT=3000

# NODE_ENV (optional, defaults to development)
# NODE_ENV=production
